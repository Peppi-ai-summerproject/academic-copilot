[pytest]
testpaths =
    backend/tests
    rag/tests
python_files = test_*.py
pythonpath =
    .
    backend
markers =
    integration: tests that require external services
    e2e: deterministic local end-to-end user journeys with external boundaries replaced
